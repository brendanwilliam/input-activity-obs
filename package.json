{
  "private": true,
  "scripts": {
    "report:dev": "node --env-file-if-exists=.env scripts/league-report-dev.mjs",
    "report:sync": "node --env-file-if-exists=.env scripts/league-report-dev.mjs --sync"
  }
}
